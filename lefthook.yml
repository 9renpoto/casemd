pre-commit:
  commands:
    gofmt:
      glob: "*.go"
      run: gofmt -w {staged_files}
    gotest:
      run: go test ./...
    typos:
      run: typos {staged_files}
    gitleaks:
      run: gitleaks protect --staged
